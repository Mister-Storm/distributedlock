<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logback config used ONLY during tests (overrides logback-spring.xml).
  Console-only: avoids file-permission issues in CI/CD and fresh clones.
-->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>

